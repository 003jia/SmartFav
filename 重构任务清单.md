# SmartFav 重构任务清单

> 范围：`background.js` 分层拆分（由「文件过胖」议题展开）
> 原始基线：`104c041`；实际执行基线：`604f892`
> 当前进度：Task 1–6 已完成，`background.js` **1396 → 248 行**
> 原则：每个任务结束后代码可运行、测试全绿、可单独 `git revert`

---

## 零、开工前先纠正三个事实

动手前需要对齐，否则会按错误前提做设计。

**1. 行数是 1396，不是 1181。** 一致性加固那轮（session 标记持久化、`saveFavoriteEntry`、写入检查）加了约 215 行。

**2. 不能按 sync-engine / trash-manager 切——会产生双向依赖。** 实测两个方向都成立：

```
restoreDeletedFavoriteUnlocked（回收站）→ SmartFavBookmarks.writeFavorite(..., getTrackedBookmarksApi())
deleteFavoriteUnlocked（同步）        → createTrashEntry(favorite, now + index)
```

根因：22 个 `withBookmarkLayoutLock` 包装器 + 22 个 `*Unlocked` 实现 + 23 处 `getStoredState()` 共享**同一个事务边界**。「删除一条收藏」在语义上就是横跨 favorites / recentlyDeleted / 浏览器书签的单个原子事务，按功能域切等于把事务劈成两半——正是一致性加固刚修掉的那类问题的翻版。所以**按层切，不按功能切**。

**3. `项目综合评估报告.md` 的 4.2（P0：跨队列并发覆盖）已经不成立，现已更新为“已关闭”。** 我把状态写入逐个映射到所属函数核对过，结论：

- 三个书签事件监听都是 `enqueueBookmarkEvent(label, () => withBookmarkLayoutLock(() => handleX(...)))`（background.js:1089、1165、1211），事件回流**在锁内**
- `restoreBookmarkLayout` / `importBookmarkRestorePoints` / `moveFavoriteToCategory` / `consumePendingBrowserActivity` 是把函数体直接包在 `withBookmarkLayoutLock(async () => {...})` 里，写入也在锁内
- **19 处写入无一例外都在布局锁内**

该报告描述的是修复前的状态。Task 4 已补齐跨入口测试，并增加一条移除布局锁后必然失败的变异哨兵；原 P0 现已形成代码与测试双闭环。

**另外要先排除一个预期：拆分不会带来任何性能收益。** 多个 `importScripts` 都是同步加载进同一个 worker，MV3 冷启动仍要重新解析全部文件。拆分只买可读性和可测性。

---

## 一、目标结构

按依赖方向自下而上，零循环：

```
constants.js（已有）
  ↑
state-store.js        116 行    getStoredState / setStoredStateChecked / setStoredFavorites / setStoredState
  ↑
bookmark-guard.js     186 行    session 内部标记 / getTrackedBookmarksApi / 两条队列 / withBookmarkLayoutLock
  ↑
favorites-service.js  965 行    22 个事务实现（本轮不再切，见 Task 5）
  ↑
background.js         248 行    importScripts + 生命周期监听 + 消息路由表
```

预期结果：`background.js` 1396 → 约 200 行；新增 3 个文件，其中下两层可直接 `require` 进 Node 单测。

---

## 二、任务清单

- [x] **Task 1：消息路由表化**（已完成）
    - [x] 1.1: 将实际存在的 **21** 个 `if (message.type === ...)` 块改写为 `MESSAGE_ROUTES`
    - [x] 1.2: 抽出统一 `routeMessage(route, message, sendResponse)`，集中处理 Promise 与错误响应
    - [x] 1.3: `onMessage` 收敛为安全查表；已知消息保持异步通道，未知消息明确返回 `false`
    - [x] 1.4: 用 `responseShape: 'raw'` 标明 `getFavorites` 的裸数组协议；其他 handler 保持 `{status}` 协议
    - [x] 1.5: 新增路由键全集、未知消息、裸数组降级读取测试；`background.js` 1396 → 1317 行；自动化全绿
    - 浏览器逐项交互不在 Task 1 重复执行，统一放在 Task 6

- [x] **Task 2：抽出 state-store.js**（已完成）
    - [x] 2.1: 已迁移 `getStoredState`、`setStoredStateChecked`、`setStoredFavorites`、`setStoredState`，保留默认值归一化
    - [x] 2.2: 使用 IIFE + `globalScope.SmartFavStateStore` + `module.exports` 双导出
    - [x] 2.3: `chrome` API 可显式注入，扩展环境默认取全局；Node 测试使用独立 mock
    - [x] 2.4: `state-store.js` 在 `constants.js` 之后、其他业务脚本之前同步加载
    - [x] 2.5: 直接单测覆盖读取、写入、无效数据归一化、读写 `lastError` 和 storage 不可用；自动化全绿

- [x] **Task 3：抽出 bookmark-guard.js**（已完成）
    - [x] 3.1: 迁移 session 标记全套（`getSessionArea` / `readSessionMarks` / `writeSessionMarks` / `prune*` / `mark*` / `consume*`）
    - [x] 3.2: 迁移 `getTrackedBookmarksApi`
    - [x] 3.3: 迁移 `bookmarkEventQueue` / `bookmarkLayoutQueue` / `enqueueBookmarkEvent` / `withBookmarkLayoutLock`
    - [x] 3.4: 队列与标记均位于 `createBookmarkGuard({ chrome })` 工厂闭包；background 只创建一个实例
    - [x] 3.5: 保留「worker 回收后队列重置、session 标记仍可恢复」的边界说明
    - [x] 3.6: 直接单测覆盖布局锁严格串行、独立实例不互相阻塞、失败后队列继续、事件队列排序、worker 重启后移除/移动标记恢复

- [x] **Task 4：补跨入口并发回归测试**（已完成）
    - [x] 4.1: `saveFavorite × onCreated`，两条记录同时保留
    - [x] 4.2: `saveFavorite × deleteFavorite` 与 `saveFavorite × onRemoved`，新增和回收互不覆盖
    - [x] 4.3: `reclassifyFavorites × onMoved`，重新分类与浏览器分类移动都保留，URL 唯一
    - [x] 4.4: `cleanupRecentlyDeleted × restoreDeletedFavorite`，覆盖两种入队顺序，只有先进入队列的操作生效
    - [x] 4.5: 所有用例使用 storage 延迟制造真实重叠；使用一条代表性变异哨兵验证移除布局锁会破坏最终不变量。无需为每个场景复制脆弱的源码变异测试
    - [x] 4.6: `项目综合评估报告.md` 4.2 已改为「原 P0 已关闭，重构必须保留单一事务边界」
    - 额外覆盖：设置补丁、分类排序补丁、浏览器活动按 ID 消费并发

- [x] **Task 5：抽出 favorites-service.js**（本轮只搬家，不切分，已完成）
    - [x] 5.1: 22 个事务入口及其实现整体迁出，存储格式与消息响应协议保持不变
    - [x] 5.2: 迁移 `createTrashEntry` / `collectRemovedUrls` / `createBrowserActivity` / `showClassificationNotification` 等辅助函数
    - [x] 5.3: `background.js` 只保留安装/启动/闹钟、三类书签事件监听、默认设置和消息路由
    - [x] 5.4: `background.js` 为 248 行；`favorites-service.js` 顶部明确单一事务边界和 Task 7 前置条件

- [x] **Task 6：全量自动验证与真实浏览器边界**
    - [x] 6.1: 拆分后的 14 个扩展 JavaScript 文件全部通过 `node --check`
    - [x] 6.2: `node tests/verify-extension.js` 单次通过，并连续 100/100 次通过
    - [x] 6.3: 测试不再预注入业务模块，而是执行 `background.js` 声明的真实 `importScripts` 链；21 条消息路由全部实际派发并做全集断言
    - [x] 6.4: R1–R6 对应的保存、双向同步、排序、备份恢复、7 天回收站、配额失败和 AI 超时路径均有自动化证据
    - [x] 6.5: README 已补充新增 3 个文件、14 文件检查命令与 1.14.4 验证口径
    - [x] 6.6: 生成 `dist/SmartFav-Edge-1.14.4.zip`；ZIP 完整，解包后的 25 个文件与源码逐字节一致
    - [x] 6.7: 已尝试连接真实 Chrome；控制接口不能接管 `chrome://extensions/` 内部页，因此未冒充完成真实点击。商店上传前仍按 `项目综合评估报告.md` 8.3 使用专用测试文件夹做五步人工冒烟

---

## 三、Backlog（不在本轮）

- [ ] **Task 7：显式事务对象，按域切 favorites-service**
  前提是把隐式全局状态访问换成 `withTransaction(async (tx) => { tx.favorites; tx.set({...}) })`，各域函数接收 `tx`。做完这一步才能按域切且不循环。半天以上，中风险，建议等有明确需求再做。

- [ ] **Task 8：拆 popup.js（当前 3407 行）**
  论「该拆」优先级高于 background.js：体量是它的 2.4 倍，且至今**零行为覆盖**。建议先抽设置/外观（~600 行）与分类规则编辑（~400 行），这两块与主收藏流程耦合最松。

- [ ] **Task 9：版本号单一来源**
  目前散在 manifest.json、popup.html 的 8 处 `?v=`、options.html、README 共 4 处。

---

## 四、风险与回溯

| 任务 | 风险 | 主要风险点 |
| --- | --- | --- |
| Task 1 | 低 | 漏掉某条消息的 `return true`，或把「裸值」handler 错包成 `{status}`，popup 侧静默拿到 undefined |
| Task 2 | 低 | `getStoredState` 的默认值合并逻辑迁移时丢字段 |
| Task 3 | 中 | 队列改闭包后，如果 background 里残留旧的全局引用，会出现「两把锁」，比不加锁更危险 |
| Task 4 | 无 | 纯新增测试 |
| Task 5 | 中 | 900 行整体搬迁，函数间调用顺序与提升行为需逐一核对 |

**回溯**：每个 Task 单独提交，`git revert <hash>` 即可。Task 2/3/5 有依赖顺序，回退需从后往前。基线 `104c041`，兜底见 `.comate/specs/storage-consistency-hardening/summary.md` 第五节。

**硬门槛**：任何一个 Task 结束时 `node tests/verify-extension.js` 不绿，不提交、不进入下一个 Task。1.14.4 集成结果为连续 100/100 次通过。
