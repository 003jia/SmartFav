# SmartFav 重构任务清单

> 范围：`background.js` 分层拆分（由「文件过胖」议题展开）
> 基线：`104c041`，`background.js` 当前 **1396 行**
> 原则：每个任务结束后代码可运行、测试全绿、可单独 `git revert`

---

## 零、开工前先纠正三个事实

动手前需要对齐，否则会按错误前提做设计。

**1. 行数是 1396，不是 1181。** 一致性加固那轮（session 标记持久化、`saveFavoriteEntry`、写入检查）加了约 215 行。

**2. 不能按 sync-engine / trash-manager 切——会产生双向依赖。** 实测两个方向都成立：

```
restoreDeletedFavoriteUnlocked（回收站）→ SmartFavBookmarks.writeFavorite(..., getTrackedBookmarksApi())
deleteFavoriteUnlocked（同步）        → createTrashEntry(favorite, now + index)
```

根因：22 个 `withBookmarkLayoutLock` 包装器 + 22 个 `*Unlocked` 实现 + 23 处 `getStoredState()` 共享**同一个事务边界**。「删除一条收藏」在语义上就是横跨 favorites / recentlyDeleted / 浏览器书签的单个原子事务，按功能域切等于把事务劈成两半——正是一致性加固刚修掉的那类问题的翻版。所以**按层切，不按功能切**。

**3. `项目综合评估报告.md` 的 4.2（P0：跨队列并发覆盖）已经不成立，需要更新那份报告。** 我把 19 处状态写入逐个映射到所属函数核对过，结论：

- 三个书签事件监听都是 `enqueueBookmarkEvent(label, () => withBookmarkLayoutLock(() => handleX(...)))`（background.js:1089、1165、1211），事件回流**在锁内**
- `restoreBookmarkLayout` / `importBookmarkRestorePoints` / `moveFavoriteToCategory` / `consumePendingBrowserActivity` 是把函数体直接包在 `withBookmarkLayoutLock(async () => {...})` 里，写入也在锁内
- **19 处写入无一例外都在布局锁内**

该报告描述的是修复前的状态。真正还没闭环的是**测试覆盖**：只有「两个 `saveFavorite` 并发」一条用例，缺「`saveFavorite` 与 `onCreated` / `delete` / `reclassify` 交叉并发」。这条并入 Task 4。

**另外要先排除一个预期：拆分不会带来任何性能收益。** 多个 `importScripts` 都是同步加载进同一个 worker，MV3 冷启动仍要重新解析全部文件。拆分只买可读性和可测性。

---

## 一、目标结构

按依赖方向自下而上，零循环：

```
constants.js（已有）
  ↑
state-store.js        ~60 行    getStoredState / setStoredStateChecked / setStoredFavorites / setStoredState
  ↑
bookmark-guard.js    ~140 行    session 内部标记 / getTrackedBookmarksApi / 两条队列 / withBookmarkLayoutLock
  ↑
favorites-service.js ~900 行    22 个事务实现（本轮不再切，见 Task 5）
  ↑
background.js        ~200 行    importScripts + 生命周期监听 + 消息路由表
```

预期结果：`background.js` 1396 → 约 200 行；新增 3 个文件，其中下两层可直接 `require` 进 Node 单测。

---

## 二、任务清单

- [ ] **Task 1：消息路由表化**（性价比最高，先做）
    - 1.1: 把 1247–1396 的 20 个 `if (message.type === ...)` 块改写为 `const MESSAGE_ROUTES = { type: handler }` 表
    - 1.2: 抽统一包装器：`route(handler, message)` 负责 `.then(sendResponse).catch(err => sendResponse({ status: 'error', message: err.message }))`
    - 1.3: `onMessage` 监听器收敿为查表 + `return true`（消除 20 处重复的 `return true`，以后加消息不会再漏）
    - 1.4: 区分「返回裸值」与「返回 `{status}`」两类 handler，用表里的标记位表示，不要在包装器里猜
    - 1.5: 验收：150 行 → 约 40 行；`node tests/verify-extension.js` 全绿；20 条消息逐个手工点一遍（见 Task 6 清单）

- [ ] **Task 2：抽出 state-store.js**
    - 2.1: 迁移 `getStoredState`（含默认值合并逻辑）、`setStoredStateChecked`、`setStoredFavorites`、`setStoredState`
    - 2.2: 沿用 IIFE + `globalScope.SmartFavStateStore` + `module.exports` 双导出，与 `constants.js` 一致
    - 2.3: 依赖注入：`chrome.storage` 通过参数传入，默认取全局，便于 Node 测试
    - 2.4: `background.js` 的 `importScripts` 在 `constants.js` 之后引入
    - 2.5: 验收：`node --check` 通过；测试全绿；新增单测覆盖「lastError 置位时 reject」（把现有 `verifyStorageWriteFailures` 里的断言下移一层）

- [ ] **Task 3：抽出 bookmark-guard.js**
    - 3.1: 迁移 session 标记全套（`getSessionArea` / `readSessionMarks` / `writeSessionMarks` / `prune*` / `mark*` / `consume*`）
    - 3.2: 迁移 `getTrackedBookmarksApi`
    - 3.3: 迁移 `bookmarkEventQueue` / `bookmarkLayoutQueue` / `enqueueBookmarkEvent` / `withBookmarkLayoutLock`
    - 3.4: 队列与标记的模块级状态改为工厂函数内的闭包（`createBookmarkGuard({ chrome })`），这样测试里可以造多个独立实例，不再靠全局互相污染
    - 3.5: 保留「worker 回收后队列重置」的注释说明，别丢掉这个已知限制
    - 3.6: 验收：测试全绿；新增单测直接对 guard 实例断言锁的串行语义（不再需要绕整个 background 起 vm）

- [ ] **Task 4：补跨入口并发回归测试**（这条独立于拆分，可先做）
    - 4.1: `saveFavorite` 与 `onCreated`（浏览器星标自动采集）并发，断言两条记录都在
    - 4.2: `saveFavorite` 与 `deleteFavorite`（不同 URL）并发，断言互不影响
    - 4.3: `reclassifyFavorites` 与 `onMoved` 并发，断言分类结果唯一且无重复条目
    - 4.4: `cleanupRecentlyDeleted` 与 `restoreDeletedFavorite`（同一 trashId）并发，断言不会「既还原又被清理」
    - 4.5: 每条用例都要验证「去掉锁则用例失败」，否则是空测试
    - 4.6: 同步更新 `项目综合评估报告.md` 的 4.2，改为「写入已全部在锁内，缺口在测试覆盖」

- [ ] **Task 5：抽出 favorites-service.js**（本轮只搬家，不切分）
    - 5.1: 把 22 个 `*Unlocked` 实现 + 其包装器整体迁出
    - 5.2: 迁移 `createTrashEntry` / `collectRemovedUrls` / `createBrowserActivity` / `showClassificationNotification` 等辅助函数
    - 5.3: `background.js` 只保留 `chrome.runtime.onInstalled` / `onStartup` / `alarms` / 三个书签事件监听 / 消息路由表
    - 5.4: 验收：`background.js` ≤ 250 行；测试全绿；`favorites-service.js` 顶部写清「本文件是单一事务边界，按域再拆需先做 Task 7」

- [ ] **Task 6：全量验证与手工回归**
    - 6.1: 11 个 js 文件 `node --check`
    - 6.2: `node tests/verify-extension.js` 全绿
    - 6.3: 浏览器加载扩展，逐个触发 20 条消息（收藏 / 删除 / 还原 / 永删 / 清理 / 重分类 / 排序 / 移动分类 / 改设置 / 备份四件套 / 一键整理 / 采集提示消费）
    - 6.4: 复跑 `summary.md` 第七节的 R1–R6 手工清单，重点 R2 双向同步与 R4 备份还原
    - 6.5: 更新 README「开发与验证」的文件清单（新增 3 个文件）

---

## 三、Backlog（不在本轮）

- [ ] **Task 7：显式事务对象，按域切 favorites-service**
  前提是把隐式全局状态访问换成 `withTransaction(async (tx) => { tx.favorites; tx.set({...}) })`，各域函数接收 `tx`。做完这一步才能按域切且不循环。半天以上，中风险，建议等有明确需求再做。

- [ ] **Task 8：拆 popup.js（3322 行）**
  论「该拆」优先级高于 background.js：体量是它的 2.4 倍，且至今**零行为覆盖**。建议先抽设置/外观（~600 行）与分类规则编辑（~400 行），这两块与主收藏流程耦合最松。

- [ ] **Task 9：版本号单一来源**
  目前散在 manifest.json、popup.html 的 8 处 `?v=`、options.html、README 共 4 处。

---

## 四、风险与回溯

| 任务 | 风险 | 主要风险点 |
| --- | --- | --- |
| Task 1 | 低 | 漏掉某条消息的 `return true`，或把「裸值」handler 错包成 `{status}`，popup 侧静默拿到 undefined |
| Task 2 | 低 | `getStoredState` 的默认值合并逻辑迁移时丢字段 |
| Task 3 | 中 | 队列改闭包后，如果 background 里残留旧的全局引用，会出现「两把锁」，比不加锁更危险 |
| Task 4 | 无 | 纯新增测试 |
| Task 5 | 中 | 900 行整体搬迁，函数间调用顺序与提升行为需逐一核对 |

**回溯**：每个 Task 单独提交，`git revert <hash>` 即可。Task 2/3/5 有依赖顺序，回退需从后往前。基线 `104c041`，兜底见 `.comate/specs/storage-consistency-hardening/summary.md` 第五节。

**硬门槛**：任何一个 Task 结束时 `node tests/verify-extension.js` 不绿，不提交、不进入下一个 Task。
