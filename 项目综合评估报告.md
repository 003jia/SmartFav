# SmartFav 项目综合评估报告

评估日期：2026-07-30

初始评估对象：SmartFav v1.14.2 工作目录、`.comate/specs/storage-consistency-hardening/` 下三份文档、自动化验证、`dist/SmartFav-Edge-1.14.2.zip` 与新版弹窗界面

初始基线：`main` 分支，`HEAD 1fcc0e8`

> **后续复核（2026-07-30）：** 第一至七节保留了 v1.14.2 的原始审查结论，便于追溯问题来源。文中两个 P0 阻断项以及 storage 读取错误处理、版本口径等相关问题，已经在 v1.14.3 发布候选中完成修复与复核。最新结论见第八节。

> **重构复核（2026-07-30）：** `codex/background-refactor-1.14.4` 已完成 Task 1–6，新增 `state-store.js`、`bookmark-guard.js`、`favorites-service.js`，并生成逐字节一致的 `dist/SmartFav-Edge-1.14.4.zip`。最新结论见第九节。

## 一、结论

SmartFav 的产品功能和新版界面已经比较完整，**当前源码适合继续本地试用和小范围验证**；但现阶段还不能下“100% 稳定、可直接以 1.14.2 对外发布”的结论。

原因不是基础功能没做完，而是还有两个发布前阻断项：

1. **当前 1.14.2 ZIP 是加固前旧包。** 它能正常解压，但不包含当前源码中的一致性加固：缺少 `constants.js`、显式 CSP、`storage.session` 内部操作标记和新的后台收藏保存入口，同时仍包含已经删除的 `styles/options.css`。
2. **收藏写入仍未完全收敛到同一个互斥队列。** 插件内“确认收藏”走 `bookmarkLayoutQueue`，但浏览器星标自动采集先在独立的 `bookmarkEventQueue` 中读取并写入 `favorites`，两条异步读—改—写路径仍可能互相覆盖。删除、恢复、重新分类等部分入口也没有统一进入布局锁。

综合判断：

| 评估项 | 结论 |
| --- | --- |
| 产品需求覆盖 | 大部分符合 |
| 本地优先、AI 可选 | 符合 |
| 当前源码自动化检查 | 通过 |
| 新版界面 | 整体良好，320px 中英文均可用 |
| 当前源码稳定性 | 可试用；仍有并发一致性风险 |
| 当前 1.14.2 ZIP | 结构完整，但内容落后于当前源码 |
| 公开发布准备度 | 有条件通过；完成 P0 项后再发新版包 |

## 二、三份 `.comate/spec` 文档审查

已阅读：

- `.comate/specs/storage-consistency-hardening/doc.md`
- `.comate/specs/storage-consistency-hardening/tasks.md`
- `.comate/specs/storage-consistency-hardening/summary.md`

### 2.1 做得好的地方

- `doc.md` 对原始 P0/P1 问题、触发条件、后果和修复路线写得比较清楚。
- “本地收藏是主要意图，浏览器写回失败不回滚本地收藏”的产品取舍合理。
- `storage.local.set` 检查 `lastError`、AI 30 秒超时、CSP、内部书签标记写入 `storage.session` 等改进已经真实存在于当前源码。
- `summary.md` 明确写出了两项边界：Promise 队列没有跨 MV3 worker 重启持久化，`dist` 中 ZIP 仍是修复前版本。这部分描述诚实且重要。
- 提交按问题拆分，具备较好的回溯条件。

### 2.2 文档与当前实现不一致

1. `summary.md` 写“11 条全部完成”，这个结论偏乐观。`background.js:928-960` 的浏览器星标自动采集仍在布局锁外写 `favorites`，没有完全消除 P0-1 同类风险。
2. `tasks.md` 的 Task 7.2～7.4 已勾选“背景图拆为独立 storage key并迁移旧数据”，但当前实现仍把 `customBackgroundImage` 放在 `settings` 内；`summary.md:188-195` 又说明这项被有意跳过。任务计划和总结相互矛盾。
3. `doc.md` 说项目当时不是 Git 仓库，这是历史背景；当前已经建立本地 Git，不过没有配置 remote。
4. `summary.md` 的商店截图仍被 Git 跟踪这一提醒已经过时。当前 `git ls-files` 中没有 `audit/`、`docs/github-assets/`、`docs/store-assets/` 和 `dist/`。
5. 手工回归清单 R1～R6 仍只是待执行清单，不能把 mock 自动化通过等同于真实 Edge/Chrome 已验证。

因此，这三份文档可以作为加固过程记录继续保留，但建议把“全部完成”改为“主体完成，仍有并发入口与发布包待收口”，并同步修正 Task 7 的勾选状态。

## 三、功能需求覆盖情况

| 功能 | 当前状态 | 证据与边界 |
| --- | --- | --- |
| 无 AI 本地分类 | 符合 | `classifier.js` 支持关键词权重、本地相似度和域名规则；AI 默认关闭 |
| AI 可选增强 | 符合 | Ollama、OpenRouter、OpenAI/OpenAI 兼容、Anthropic 兼容等入口存在；失败不阻断本地收藏 |
| 中英文界面与商店本地化 | 符合 | `_locales/zh_CN`、`_locales/en`、`i18n.js`；320px 中英文界面实测无横向溢出 |
| 自定义分类和关键词 | 符合 | 支持新建分类、修改规则、英文逗号及多种分隔符、空格与双引号词组 |
| 新规则重新分类已有收藏 | 符合 | `reclassifyFavorites()` 已实现；自动化包含分类相关验证 |
| 分类/书签拖动排序 | 符合 | 鼠标拖动与 `Alt + 方向键` 路径存在，自动化覆盖排序 |
| 单条书签跨分类移动 | 符合 | SmartFav 内移动与浏览器受管书签移动均有实现和 mock 验证 |
| 最近删除与 7 天清理 | 符合 | 恢复、永久删除、alarm 清理均已实现 |
| 浏览器收藏新增/覆盖 | 符合 | add/overwrite 模式存在并有 mock 验证 |
| 浏览器星标自动分类与通知 | 主体符合 | 代码和 mock 流程存在；真实浏览器通知尚未在本轮验证 |
| 浏览器删除实时同步到最近删除 | 主体符合 | `onRemoved` 流程存在并覆盖重复 URL、文件夹递归删除；仍需真实浏览器回归 |
| 浏览器跨文件夹移动回流 | 主体符合 | `onMoved` 流程进入布局锁；真实浏览器回归尚未执行 |
| 整理前布局备份与还原 | 主体符合 | 创建、预览、恢复、导入、导出及故障用例存在；本轮没有对真实个人书签做破坏性回归 |
| 五种风格、明暗模式、背景图、宽高 | 符合 | 新版界面实测；黑色切换到其他风格时可回到亮色 |
| AI 分析文件夹并填写关键词 | 主体符合 | 抽样、分批、脱敏、校验和确认后保存均有实现；未使用真实外部 API 做网络验证 |

## 四、稳定性与代码审查

### 4.1 已确认通过

- 所有源码及测试 JavaScript 执行 `node --check`，退出码为 0。
- `node tests/verify-extension.js` 输出 `SmartFav extension verification passed.`。
- 自动化覆盖以下主要模块：
  - Manifest 与中英本地化
  - 本地分类与手动排序
  - OpenAI/Anthropic/Ollama 请求结构
  - AI 关键词建议
  - 浏览器书签新增/覆盖、排序、整理
  - 备份、预览、恢复、导入导出
  - 浏览器新增、移动、删除回流
  - 并发执行两个 `saveFavorite`
  - storage 配额写入失败
  - 内部操作标记写入 `storage.session`
- `unzip -t dist/SmartFav-Edge-1.14.2.zip` 无压缩结构错误。
- 源码静态隐私扫描未发现真实 API Key、密码、本机用户路径或 Comate 内容；仅命中测试夹具中的 `openai-secret`、`anthropic-secret`。
- 本地预览的控制台未发现 error/warning。

### 4.2 已关闭：收藏状态跨入口并发覆盖

这一节在最初审查时记录的是修复前状态，v1.14.3 已完成收口：

- `bookmarkEventQueue` 只维护浏览器事件顺序，不再独立承担状态事务。
- 所有会读改写 `settings`、`favorites`、`favoriteOrder`、`recentlyDeleted`，或改变浏览器书签布局的入口，均进入同一个 `bookmarkLayoutQueue`。
- 三个浏览器书签事件监听都以 `enqueueBookmarkEvent(..., () => withBookmarkLayoutLock(...))` 进入统一事务边界。
- 设置和排序改用后台补丁合并，浏览器活动按 ID 消费，避免旧弹窗清除新活动。

自动化已经覆盖：

- 两个 `saveFavorite` 并发。
- `saveFavorite × onCreated`、`saveFavorite × deleteFavorite`、`saveFavorite × onRemoved`。
- `reclassifyFavorites × onMoved`。
- `cleanupRecentlyDeleted × restoreDeletedFavorite` 的两种先后顺序。
- 不同设置字段、不同分类排序和过期活动消费并发。

测试通过 storage 延迟制造真实读—改—写重叠，并包含一条“移除布局锁后最终不变量必然失败”的变异哨兵，防止并发测试只是在顺序执行。当前结论：**原 P0 已关闭；后续重构必须继续保留单一布局事务边界。**

### 4.3 P0：发布包与当前源码不一致

当前 ZIP SHA-256：

```text
e7655f8a1f316c2c9f560b76d8d3220f972ed8dc28331643e66562a1b4a76443
```

源码与解压后的 ZIP 对比结果：

- `ai-client.js` 不同
- `background.js` 不同
- `i18n.js` 不同
- `manifest.json` 不同
- `popup.html`、`popup.js`、`styles/popup.css` 不同
- ZIP 缺少 `constants.js`
- ZIP 仍包含 `styles/options.css`
- ZIP 的 Manifest 没有当前源码中的 `content_security_policy`

所以 ZIP “能解压”只证明压缩结构没坏，不能证明它包含当前修复。继续从 `dist/SmartFav-Edge-1.14.2.zip` 安装，实际得到的是旧逻辑。

### 4.4 P1：真实浏览器与 MV3 生命周期还未形成闭环证据

- 本轮 UI 验证是在本地预览模式进行的，浏览器书签、通知、alarms 和 `storage.session` 使用 mock 或降级路径。
- `bookmarkEventQueue` 与 `bookmarkLayoutQueue` 是 worker 内存 Promise 链。worker 在任务边界被回收时，未开始的内存任务不能重放；文档已经正确把它列为遗留项。
- 布局恢复会实际移动用户书签，本轮没有在真实个人书签环境执行破坏性验证。

因此真实 Edge/Chrome 至少还应执行文档中的 R1～R6，并补一组“暂停/重启 service worker 后继续新增、移动、删除书签”的回归。

### 4.5 P1：当前本地仓库没有远程发布链路

当前 `.git/config` 没有 remote。当前加固提交虽然在本地 Git 中可回溯，但无法从这个工作目录证明它们已经合并到正式 GitHub 仓库。后续接远程时应先核对远端历史，不建议直接强推或覆盖。

### 4.6 P2：可继续收口的问题

- `popup.html:7` 的 CSS 查询串是 `v=1.15.0`，其他脚本和 Manifest 仍是 `1.14.2`，版本口径不一致。
- `background.js:296-324` 与 `popup.js:328-335` 的 storage 读取没有统一检查 `chrome.runtime.lastError`；写入已检查，读取仍可加强。
- `tasks.md` 声称背景图已拆出独立 key，但当前仍随整份 settings 存储。800KB 原文件转 base64 后约占 1.07MB，最好明确这里的预算口径。
- `background.js:283-290` 的首次安装初始化使用原生 `storage.local.set`，没有检查 `lastError`。
- `popup.js` 3340 行、`popup.css` 2439 行，单文件较大。当前能运行，但会增加后续修改和回归成本；适合在稳定后按“导航/收藏/分类/设置”逐步拆分。
- 测试覆盖面已经明显改善，但仍包含较多源码正则契约，不能替代 DOM 交互和真实浏览器集成测试。

## 五、新版界面审查

### 5.1 总体评价

新版界面已经比早期版本成熟：

- 首页、我的收藏、分类文件夹和设置分成独立视图，主页不再堆叠全部内容。
- 收藏卡片、数量徽标、删除和移动入口保持统一。
- 本地分类是主流程，AI 入口没有喧宾夺主。
- 设置采用紧凑滚动面板，自动保存的语义明确。
- 亮色、暗色与 320px 中英文状态均没有发现布局破裂或横向滚动。
- 使用了标题、区域、按钮、标签和 `aria-live`；也存在 `prefers-reduced-motion` 处理。

仍可优化：

- 头部文字按钮约为 `44 × 30px`，高度低于常见的 44px 无障碍触控建议；桌面鼠标可用，但可把点击区域加高而不必放大文字。
- 部分辅助说明使用 11px 字号，320px 英文设置页信息密度较高。
- 英文设置面板内容高度约 1109px，而可视高度约 504px，需要滚动两屏以上；可以继续合并重复说明或使用折叠分组。
- 毛玻璃在空白本地预览背景上只能确认面板样式，不能完整判断真实网页内容透过浏览器弹窗时的视觉表现。

### 5.2 审查步骤与截图

#### 步骤 1：中文首页，360 × 560，亮色毛玻璃

健康度：良好。品牌、主操作、状态与底部导航层级清楚。

![中文首页亮色](audit/2026-07-30/01-home-light.png)

#### 步骤 2：我的收藏，360 × 560

健康度：良好。收藏条目、域名和操作区域保持一致，返回路径明确。

![我的收藏](audit/2026-07-30/02-favorites-light.png)

#### 步骤 3：分类文件夹，360 × 560

健康度：良好。分类管理、关键词说明和 AI 辅助之间的边界清楚。

![分类文件夹](audit/2026-07-30/03-categories-light.png)

#### 步骤 4：中文设置，360 × 560，亮色

健康度：良好但偏长。浏览器同步、界面与 AI 设置都能找到，建议继续优化说明密度。

![中文设置亮色](audit/2026-07-30/04-settings-light.png)

#### 步骤 5：中文设置，360 × 560，暗色

健康度：良好。对比度和控件状态连贯，黑色风格切回其他风格时亮暗模式会恢复。

![中文设置暗色](audit/2026-07-30/05-settings-dark.png)

#### 步骤 6：中文首页，320 × 560

健康度：通过。最窄配置下没有横向溢出，主要操作仍完整。

![中文首页 320px](audit/2026-07-30/06-home-320px.png)

#### 步骤 7：英文首页，320 × 560

健康度：通过。英文标题和导航未被裁切。

![英文首页 320px](audit/2026-07-30/07-home-en-320px.png)

#### 步骤 8：英文设置，320 × 560

健康度：可用但信息密度偏高。无横向溢出，长说明需要较多纵向滚动。

![英文设置 320px](audit/2026-07-30/08-settings-en-320px.png)

## 六、发布前建议顺序

### P0：必须先完成

1. 所有收藏、回收站和排序状态写入统一进入同一个后台锁/事务入口。
2. 新增跨路径并发测试：`saveFavorite × onCreated`、`saveFavorite × onRemoved`、`delete × restore`、`reclassify × autoCapture`。
3. 明确下一个版本号。若本次是稳定性补丁，建议 `1.14.3`；若正式采用当前 `v=1.15.0` 界面口径，则统一为 `1.15.0`。
4. 从当前源码重新生成干净 ZIP，并设置包内容断言：
   - 必须包含 `constants.js`
   - 必须包含 CSP
   - 不得包含 `styles/options.css`
   - 解包后关键源码应与发布源一致
5. 连接并核对正式 GitHub 历史后，再合并和发布，不直接覆盖未知远端历史。

### P1：正式发布前完成

1. 在真实 Edge 和 Chrome 各执行一次 R1～R6。
2. 增加 MV3 worker 停止/重启后的新增、移动、删除回归。
3. 使用独立测试收藏夹验证备份与还原，不直接拿个人主收藏夹做首轮测试。
4. 更新 `.comate/spec` 的完成状态、背景图方案和遗留项。

### P2：后续迭代

1. storage 读失败统一显式处理。
2. 把背景图存储预算与实现写清楚，必要时拆为独立 key。
3. 统一版本号和缓存查询串。
4. 逐步拆分超大的 popup 脚本与样式文件。
5. 将头部按钮点击高度提升到 36～44px，辅助说明尽量不低于 12px。

## 七、建议验收门槛

满足以下条件后，可以把下一版标为“稳定发布候选”：

- 跨入口并发测试连续重复执行无收藏丢失。
- 全部 `node --check` 与 `verify-extension.js` 通过。
- 新 ZIP 的 Manifest、文件列表和关键源码与当前发布源一致。
- Edge/Chrome R1～R6 有逐项结果记录。
- worker 重启后内部操作不会产生重复通知、误删除或状态丢失。
- 使用 320px/360px、中英文、亮暗模式各完成一次界面回归。
- GitHub remote、tag、Release ZIP 与 Manifest 版本号一致。

最终建议：**当前源码可以继续使用和测试；不要继续把现有 1.14.2 ZIP 当成“已包含稳定性加固”的正式包。先修并发收口、重新打包并完成真实浏览器回归，再发布下一版。**

## 八、v1.14.3 修复复核

### 8.1 已完成的发布阻断项

| 原问题 | v1.14.3 处置与复核结果 |
| --- | --- |
| 收藏状态存在跨队列覆盖风险 | 所有会修改 `favorites`、`recentlyDeleted`、`settings`、`favoriteOrder` 和待展示浏览器活动的关键路径，统一由后台串行事务处理 |
| 浏览器星标与插件保存可能并发丢数据 | 新增 `saveFavorite × onCreated`、`saveFavorite × delete`、`saveFavorite × onRemoved` 等跨入口并发用例，连续 100 轮验证通过 |
| 设置和排序采用整对象写入 | 改为后台合并补丁；不同设置字段、不同分类顺序并发更新均有回归用例 |
| 旧通知可能清除新通知 | 按活动 ID 消费，过期界面不能再清除更新后的浏览器活动 |
| storage 读取错误未统一处理 | 后台与弹窗读取均检查 `chrome.runtime.lastError`；读失败用例确认不会覆盖既有数据 |
| 版本号与缓存查询串不一致 | Manifest、弹窗、设置页、README 和发布说明统一为 `1.14.3` |
| ZIP 落后于源码 | 重新生成 `dist/SmartFav-Edge-1.14.3.zip`，解包后的 22 个文件与发布源码逐字节一致 |

### 8.2 最终验证结果

- 全部扩展源码和测试 JavaScript 均通过 `node --check`。
- `node tests/verify-extension.js` 单次通过，并连续执行 100 轮无失败。
- `node tests/verify-release-package.js` 确认 ZIP 文件列表、Manifest、CSP 和全部文件内容与源码一致。
- `unzip -t dist/SmartFav-Edge-1.14.3.zip` 通过。
- 发布源静态扫描未发现 API Key、密码、本机路径或其他隐私内容。
- 360px 与 320px、中文与英文、亮色与暗色、黑色切回白色、首页/收藏/分类/设置等主要界面路径均通过预览回归；控制台无 error 或 warning。
- 本轮没有生成独立 SHA-256 文件，安装目录中也没有测试截图、历史包、`.git`、`.DS_Store` 或 `__MACOSX`。

### 8.3 真实浏览器边界

本轮尝试使用隔离的临时 Chrome/Edge 配置加载扩展，未访问或修改个人书签。当前自动化环境不能接管 `chrome://extensions/` 内部页；品牌版 Chrome/Edge 的无界面进程也拒绝旁加载扩展。因此，下列动作仍需在商店安装版或“加载已解压扩展”的真实浏览器中做最后一次人工冒烟：

1. 新建一个专用测试书签文件夹。
2. 通过浏览器星标新增、移动和删除一条测试书签，确认 SmartFav 分类、通知和最近删除同步。
3. 通过 SmartFav 新增、跨分类移动和删除一条测试书签，确认浏览器书签同步。
4. 重载扩展后重复一次新增和删除，确认 MV3 service worker 重启后仍正常。
5. 只对专用测试文件夹执行一次整理、备份预览和恢复，确认结果后删除测试数据。

### 8.4 最新结论

**v1.14.3 已达到“稳定发布候选”标准：在当前可自动验证范围内，已知 P0 阻断项清零，源码、状态一致性、界面和发布包全部通过。** 软件无法对所有浏览器版本、第三方 API、网络状态和用户书签结构作数学意义上的 100% 保证；正式公开发布前只剩上面的真实浏览器五步人工冒烟，不应把这项环境边界隐藏成“已自动验证”。

---

## 九、v1.14.4 后台分层重构复核

### 9.1 结构结果

| 层 | 行数 | 职责 |
| --- | ---: | --- |
| `state-store.js` | 116 | 状态默认值归一化、读写与 storage 错误处理 |
| `bookmark-guard.js` | 186 | session 内部标记、浏览器事件队列、唯一布局事务锁 |
| `favorites-service.js` | 965 | 收藏、同步、回收站、排序、整理与备份的单一事务边界 |
| `background.js` | 248 | 生命周期、三类书签事件监听与 21 条消息路由 |

`background.js` 只创建一个 `bookmarkGuard`，三个浏览器事件监听和全部收藏业务共享同一个 `withBookmarkLayoutLock`。没有残留第二套队列，也没有按功能域劈开跨 `favorites`、`recentlyDeleted` 与浏览器书签的原子操作。

### 9.2 R1–R6 自动化证据

| 回归项 | 自动化覆盖 |
| --- | --- |
| R1 收藏保存 | 本地保存、开启同步、同网址覆盖、手动分类学习及撤销；并发覆盖 `saveFavorite × onCreated/delete/onRemoved` |
| R2 双向同步 | 浏览器新增、跨分类移动、删除最后一份同网址书签、文件夹递归删除和内部操作标记吞回流 |
| R3 排序写回 | 分类和分类内书签排序写回、重开状态持久化、跨分类顺序合并 |
| R4 备份还原 | 创建、预览、恢复、重复恢复、损坏导入拒绝、活动还原点不被上限裁掉 |
| R5 回收站过期 | 恢复、永久删除、7 天过期清理、清理与恢复两种并发顺序 |
| R6 配额与超时 | storage 读写失败保持原状态；800KB 背景图限制存在；AI abort 路径断言 30 秒超时错误 |

测试夹具会执行 `background.js` 的真实 `importScripts` 文件链，不再以预注入模块绕过加载过程；21 条消息类型全部实际派发并在套件结束时做全集断言。

### 9.3 验证结果

- 14 个扩展 JavaScript 文件全部通过 `node --check`。
- `node tests/verify-extension.js` 连续 100/100 次通过。
- `unzip -t dist/SmartFav-Edge-1.14.4.zip` 通过。
- `node tests/verify-release-package.js dist/SmartFav-Edge-1.14.4.zip` 确认 25 个文件与当前扩展源码逐字节一致。
- Manifest、popup、options、README 和发布说明的版本口径统一为 1.14.4。
- 存储键、21 条消息协议、首次安装默认分类词表和本地优先行为保持兼容。

### 9.4 剩余发布边界

已再次连接真实 Chrome 并尝试接管扩展管理页，但浏览器控制接口明确拒绝访问 `chrome://extensions/` 内部页。没有读取 Chrome 配置，也没有操作用户个人书签。第 8.3 节的五步人工冒烟仍是商店提交前的真实环境检查项，不属于已自动完成证据。

### 9.5 最新结论

**v1.14.4 的重构实现、自动化回归和安装包已经完成；当前没有已知代码级发布阻断项。** 该结论不等于对所有浏览器版本、个人书签结构、网络和第三方 AI 服务作数学意义上的 100% 保证，正式公开发布前仍应执行第 8.3 节的专用测试书签人工冒烟。
